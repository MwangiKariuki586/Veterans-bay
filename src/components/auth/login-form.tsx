"use client";

import { AuthFlipPanel } from "@/components/auth/auth-flip-panel";

/** @deprecated Prefer AuthFlipPanel — kept for existing imports. */
export function LoginForm() {
  return <AuthFlipPanel />;
}
