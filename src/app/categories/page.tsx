import { createUnavailableRoute } from "@/components/public/create-unavailable-route";

export default createUnavailableRoute({
  eyebrow: "Categories",
  title: "Browse service categories.",
  description:
    "Category browsing will open with the marketplace discovery phase.",
});
