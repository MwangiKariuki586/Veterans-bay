export const storagePermissions = {
  uploadIntent: "storage.upload.intent",
  uploadComplete: "storage.upload.complete",
  delivery: "storage.delivery",
  link: "storage.link",
  replace: "storage.replace",
  delete: "storage.delete",
  cleanup: "storage.cleanup",
} as const;
